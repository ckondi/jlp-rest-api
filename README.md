## John Lewis Rest API Service for Dresses

### Build Locally:
```shell script
./gradlew clean build
```

### Run application Locally:
```shell script
./gradlew bootRun
```

Provided the application is running the end point is accessible at

`http://localhost:8080/jlp-rest-api/api/v1/dresses/sale?priceLabel=<label>`
